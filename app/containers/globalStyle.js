import {StyleSheet} from 'react-native';

const GlobalStyle = StyleSheet.create({
    container: {
        flex: 1
    },
    simpleBottomBorder: {
        borderColor: '#e1e3df',
        borderBottomWidth: 1
    }
});

export default GlobalStyle;